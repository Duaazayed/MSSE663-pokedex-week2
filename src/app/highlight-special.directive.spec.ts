import { HighlightSpecialDirective } from './highlight-special.directive';

describe('HighlightSpecialDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSpecialDirective();
    expect(directive).toBeTruthy();
  });
});
